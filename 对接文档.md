# MyRobot 项目对接文档（交接给下一个 AI 智能体）

> 生成：2026-08-20 ｜ 仓库：https://github.com/A-CEL-ZPC/My_library（分支 main）
> 用途：让下一个智能体无需重读全部对话即可接手。请先读本文档，再读 `进度.md` 和 `README.md`。

---

## 0. 紧急状态一句话

**项目主体（mock 仿真 + MoveIt + 抓取演示）全部可用、已提交；Gazebo 集成差"最后一步端到端验证"**——版本兼容补丁已全部完成并编译通过，但受当前沙箱限制无法完整运行 Gazebo，需要换环境验证。详见第 5、6 节。

---

## 1. 项目速览

- **目标**：ROS 2 Humble 六自由度机械臂（6 轴 + 二指夹爪）运动控制，从仿真走向物理仿真（Gazebo）与抓取应用
- **技术栈**：ROS 2 Humble + MoveIt 2 (2.5.9) + ros2_control + Gazebo Classic 11
- **架构**：`myrobot.launch.xml` 一键启动 RSP + ros2_control_node + 3 控制器 + move_group + commander + rviz2
- **控制面**：3 个话题（`/joint_command` 关节角、`/pose_command` 位姿+笛卡尔、`/open_gripper` 夹爪）+ 1 个演示程序（`grasp_demo` pick-and-place）

## 2. 环境信息（重要，很多问题的根源）

| 项 | 值 | 备注 |
|---|---|---|
| 系统 | Ubuntu 22.04 (jammy)，用户 `zhz` | 桌面环境有显示（rviz/gazebo 可弹窗） |
| 工作区 | `/home/zhz/robot_arm_ws` | **git 仓库根目录** |
| ROS | Humble（官方源 packages.ros.org） | |
| **ros2_control** | **2.54.0** ⚠️ | 官方 Humble 源回移植的新版（非 Humble 标准 2.16/2.17），与 gazebo_ros2_control 0.4.10 不兼容（详见第 5 节） |
| moveit | 2.5.9 | 正常 |
| gazebo_ros2_control | 0.4.10（旧） | 需源码补丁，已打好在工作区 |
| Gazebo | 11 + gazebo_ros 3.x | 正常 |
| 编辑器 | VSCode（snap 版） | 用户习惯；C/C++ 扩展曾缺失，已手动安装 |
| Git 远程 | `git@github.com:A-CEL-ZPC/My_library.git`（SSH） | 密钥 `~/.ssh/id_ed25519` 已配好 |

**沙箱约束（智能体执行命令时）**：
- 文件沙箱只读 `/home/zhz` 除工作区外 → **所有会写 `~/.ros`、`~/.gazebo` 的命令必须 `export HOME=/home/zhz/robot_arm_ws/.xxx` 覆盖**（gzserver 不覆盖 HOME 会因 `~/.gazebo` 只读崩溃）
- 沙箱内 `ros2 topic pub/echo` 等 CLI 工具**不可靠**（DDS 消息流不通、会挂起）→ 验证消息用 **Python 发布器**（rclpy）或看进程日志
- snap 版 `code` 在沙箱内无法运行（snap-confine 权限）
- 网络：github.com 慢但可用（SSH 22 端口通、api.github.com 通、codeload 下载可用）

## 3. 仓库结构

```
robot_arm_ws/
├── src/
│   ├── myrobot_description/      # 模型（唯一源）
│   │   ├── urdf/                 # myrobot_urdf.xacro + arm/gripper/ros2_control 宏 + initial_positions.yaml
│   │   ├── launch/display.launch.xml
│   │   └── rviz/urdf_config.rviz
│   ├── myrobot_bringup/          # mock 仿真一键启动（launch + 控制器 yaml + rviz 配置）
│   ├── myrobot_moveit_config/    # MoveIt 配置（SRDF/kinematics/joint_limits/moveit_controllers/launch）
│   ├── myrobot_interfaces/       # 自定义消息 PoseCommand
│   ├── myrobot_commander_cpp/    # C++ 控制程序
│   │   ├── src/test_moveit.cpp       # 入门示例
│   │   ├── src/commander_template.cpp# 话题控制模板（3 订阅）
│   │   └── src/grasp_demo.cpp        # pick-and-place 演示
│   ├── myrobot_gazebo/           # Gazebo 仿真包（world/launch/config）
│   └── gazebo_ros2_control/      # ⚠️ 源码补丁包（0.4.10 + 3 处适配补丁，见第 5 节）
├── stop_all.sh                   # 一键清理残留进程
├── 进度.md / README.md           # 进度与说明
└── .vscode/                      # c_cpp_properties.json（VSCode 补全）
```

**构建规范**：`cd /home/zhz/robot_arm_ws && source /opt/ros/humble/setup.bash && colcon build --packages-select <pkg>`，然后 `source install/setup.bash`。**绝不在家目录或 src/ 下跑 colcon build**（会生成遮蔽性 install，见踩坑 #3）。

## 4. 已完成进度（验证状态）

| 项 | 状态 | 最后验证 |
|---|---|---|
| mock 全链路（launch→控制器→move_group） | ✅ | 多次实测：3 控制器 Configured and activated |
| `/joint_command` 关节控制 | ✅ | commander 日志：规划成功→执行成功 |
| `/pose_command` 位姿控制 | ✅ | 同上 |
| `/open_gripper` 夹爪 | ✅ | move_group：Received request → SUCCEEDED |
| `grasp_demo` 8 步抓取 | ✅ | 全 8 步 ✅（提交 89a245f） |
| 模型统一 + sim_mode 切换 | ✅ | xacro 两种模式输出正确 |
| 惯性参数（11 link） | ✅ | URDF 解析通过 |
| Gazebo 插件兼容补丁 | ✅ 编译+单测 | 见第 5 节 |
| Gazebo 完整 launch 运行 | ⏳ **未验证** | 卡沙箱，需用户终端验证 |

## 5. Gazebo 集成：当前状态与修复方案（详细）

### 5.1 问题根源

官方 Humble 源把 ros2_control 升级到 **2.54.0**，但 gazebo_ros2_control 仍是 **0.4.10**：
- 2.54 的 ResourceManager 只认 `hardware_interface::SystemInterface` 基类（`libhardware_interface.so` 中已无 GazeboSystemInterface 符号）
- 0.4.10 的插件注册在旧基类 `gazebo_ros2_control::GazeboSystemInterface` 下 → **插件找不到**
- 官方源没有配套新版 gazebo_ros2_control（apt 无候选）

### 5.2 修复方案（已完成，在工作区 `src/gazebo_ros2_control/`）

从 GitHub 拉取 0.4.10 源码（codeload tarball），打 3 处补丁：

1. **`gazebo_ros2_control/gazebo_hardware_plugins.xml`**：`base_class_type` 改为 `hardware_interface::SystemInterface`
2. **`gazebo_ros2_control/CMakeLists.txt`**：增加 `pluginlib_export_plugin_description_file(hardware_interface gazebo_hardware_plugins.xml)`（旧键保留）
3. **`gazebo_ros2_control/src/gazebo_system.cpp`**：`PLUGINLIB_EXPORT_CLASS(GazeboSystem, hardware_interface::SystemInterface)`
   —— **这是关键**：class_loader 的工厂注册按"基类"分桶（`getFactoryMapForBaseClass<Base>()`），只改 XML 不够，注册宏的基类必须同步改

**排查该问题的完整思路（可复用）**：
- `pluginlib::LibraryLoadException`（找不到）→ 查 pluginlib 索引 `ament_index/resource_index/hardware_interface__pluginlib__plugin/`（需 package.xml export 或 CMake 的 pluginlib_export_plugin_description_file）
- `CreateClassException`（找到但无法创建）→ 用最小 C++ 测试程序（`pluginlib::ClassLoader<SystemInterface>` + `createUniqueInstance`）拿到真实底层错误
- 最终发现工厂按基类分桶 → 改注册宏

### 5.3 架构要点（0.4.10 的 gazebo 工作方式）

- URDF 需含 `<gazebo><plugin name="gazebo_ros2_control" filename="libgazebo_ros2_control.so"><parameters>...yaml</parameters></plugin></gazebo>`（已在 myrobot_urdf.xacro 中按 sim_mode 条件生成）
- **gazebo 插件在 gzserver 内部自建 ControllerManager**（节点名 controller_manager）→ **不需要外部 ros2_control_node**（gazebo.launch.xml 已删掉）
- 控制器用 spawner 加载（本版本 spawner 默认等待重试，无需 --wait 参数）

### 5.4 已验证/未验证

✅ 已验证（沙箱内）：
- 插件被发现、`ros2_control_node` 加载并初始化硬件 `Arm`（隔离测试：URDF sim_mode:=gazebo 直接 -p robot_description）
- gzserver 手动启动（HOME 覆盖后）→ `/spawn_entity` 服务出现
- xacro gazebo 模式正确生成插件块

⏳ 未验证（必须在用户终端做）：
- `ros2 launch myrobot_gazebo gazebo.launch.xml` 完整运行：spawn 成功 → gzserver 内 controller_manager 出现 → spawner 加载 3 控制器 → move_group 就绪
- **预期问题**：grasp_demo 的抓取坐标（0.65,0,0.30 等）是给 mock 环境调的；gazebo 世界里方块在桌上（z≈0.45），坐标需重新调整（抓取 z≈0.45-0.48，预抓取再高 0.1+）

## 6. 下一步行动清单（给下一个智能体）

1. **在用户终端**跑 `ros2 launch myrobot_gazebo gazebo.launch.xml`（gui 默认开），检查日志；失败则贴日志排查（重点：spawn 是否成功、controller_manager 服务是否出现）
2. 通过后跑 `ros2 run myrobot_commander_cpp grasp_demo`，验证物理环境运动；调整抓取坐标（按 gazebo 世界桌面 0.4m、方块 0.05m 计算）
3. 清理：跑 `./stop_all.sh`（或用 comm 截断名杀进程，见操作规范）
4. 提交 git：`git add -A && git commit -m "..." && git push`
5. 后续方向（mock 链路已验证，可并行）：MoveIt 规划场景避障、视觉（AprilTag）等——见 进度.md

## 7. 踩坑记录（对话中出现的所有问题，按主题）

### 7.1 ROS 2 launch / XML
1. **顶层 `<param>` 报 `Unrecognized entity of the type: param`**：ROS2 的 `<param>` 只能写在 `<node>` 内；顶层是 ROS1 写法。第二个报错 `SyntaxError (line 1)` 是 launch 兜底把 XML 当 Python 解析的假错误，忽略
2. **`$(command 'xacro ...')` 对 stderr 敏感**：被调命令只要往 stderr 打印任何内容（含弃用警告）就报 `executed command showed stderr output`。因此 xacro 里要用 `xacro.load_yaml()` 而非弃用的 `load_yaml()`
3. **launch 文件从 install/ 加载**：改 `src/` 下的 launch 必须 `colcon build` 同步到 install/，否则跑旧文件

### 7.2 工作区/环境
4. **双工作区遮蔽（最坑的一次）**：误在家目录跑 `colcon build` 生成 `~/install`（含旧包副本）；之后 `robot_arm_ws/install/setup.bash`（prefix chain）把 `~/install` 链入且排在最前 → `ros2 pkg prefix myrobot_bringup` 解析到旧副本，所有修复"无效"。修复：移走 `~/install` 备份 + 干净环境重新 build 重生成 chain。**教训：永远在 workspace 根目录 build**
5. **进程名 15 字符截断**：`ps -eo comm` 显示 `ros2_control_no`/`robot_state_pub`/`commander_templ`（截断），用完整名做精确匹配永远匹配不上 → 清理脚本必须用截断名或 `pkill -f`（带 `[x]` 方括号防自匹配）
6. **Ctrl+C 杀不干净 launch 子进程**：move_group 关闭慢/段错误、关窗口不传信号 → 残留进程堆叠导致"话题有 6 个订阅者、发布没反应"。解法：`stop_all.sh`；DDS 幽灵订阅可用 `ros2 daemon stop && ros2 daemon start` 清掉
7. **VSCode 无 C++ 补全**：根因是 `ms-vscode.cpptools` 扩展本体没装（只有扩展包）→ 从 marketplace 下 VSIX 手动解压到 `~/.vscode/extensions/`；另外 `.vscode/c_cpp_properties.json` 需指向 `build/<pkg>/compile_commands.json`（colcon build 后更新）
8. **`rm -r` 误删 src**：靠 `git restore src/` 恢复；未提交的改动会丢（当时 joint_command 功能靠回忆补回）。**教训：改完能跑就提交**

### 7.3 C++ / CMake / 消息
9. **`.h` vs `.hpp` 消息头**：ROS2 生成的 C 接口是 `.h`（`example_interfaces__msg__Bool` 结构体），**C++ 接口是 `.hpp`**（`example_interfaces::msg::Bool`）。自定义消息 C++ 头是小写蛇形（`pose_command.hpp`），不是 `PoseCommand`
10. **`find_package` ≠ 依赖**：include 路径由 `ament_target_dependencies` 添加，两者缺一不可；package.xml 缺 `<depend>` 会导致 colcon 构建顺序错误
11. **`main(int argc, char ** argv)`**：`rclcpp::init` 需要二级指针；`char* argv`/`char const * argv` 都编译不过
12. **`computeCartesianPath` 参数数**：ROS1 教程的 3 参写法在 MoveIt 2 不行（jump_threshold 参数保留但必须占位）→ `(waypoints, eef_step, 0.0, trajectory)`
13. **`setPoseTarget` 与 `setJointValueTarget` 目标混合**：先位姿后关节会导致规划失败（目标自相矛盾）→ 每次设目标前 `clearPoseTargets()`
14. **消息字段 YAML 冒号后必须有空格**：`"{data:false}"` 会把 `data:false` 当键名；`flase` 拼错会被当字符串→bool 字段转成 `True`（危险静默错误）。发布后看回显即可发现
15. **URDF `<mass>` 必须用 value 属性**：`<mass value="5.0"/>`，不是 `<mass>5.0</mass>`（那是 SDF 写法）
16. **`double` 不是 ROS2 消息类型**：双精度是 `float64`

### 7.4 MoveIt / 运行
17. **move_group 未就绪时规划会卡住**：等 `You can start planning now!` 再发指令
18. **move_group Ctrl+C 段错误**：MoveIt 已知问题，忽略
19. **mock 硬件关节状态不变**：排查过 GenericSystem 源码（`read()` 默认 state=command，除非 `disable_commands` 参数）——当时"不动"实为残留进程抢消息，非硬件问题
20. **`/joint_states` 多系统混发**：多套残留系统同时发布 → 数据来源混乱；用 `ROS_DOMAIN_ID` 隔离测试可排除

### 7.5 Git / 工具
21. **git 身份**：已配 `ASan <3471308991@qq.com>`；SSH 密钥 `~/.ssh/id_ed25519`，公钥已加 GitHub；推送用 `git push`（SSH，无需密码）
22. **GitHub 网络**：HTTPS 慢/挂起，**SSH 22 端口通畅**（务必用 SSH remote，不要改回 HTTPS）

## 8. 给下一个智能体的操作规范

1. **验证后必须清理**：跑完 launch/验证，执行 `./stop_all.sh` + `ps -eo pid,comm | awk '$2=="gzserver"||$2=="ros2_control_no"||...'` 复查为零再回复用户（用户明确抱怨过留窗口）
2. **HOME 覆盖**：任何会写 `~/.ros`/`~/.gazebo` 的命令先 `export HOME=/home/zhz/robot_arm_ws/.tmp_xxx && mkdir -p $HOME`；注意 `export && cmd &` 会把整个链后台化导致 HOME 丢失（用 `(cmd & echo $! > pid)` 结构）
3. **杀进程**：用 comm 截断名（`ros2_control_no`/`robot_state_pub`/`commander_templ`）或 `pkill -f` + 方括号技巧；不要在一条命令里同时包含目标模式文本和 pkill（会自杀）
4. **消息验证**：沙箱内 `ros2 topic pub` 不可靠 → 用 Python rclpy 发布器（参考历史：`create_publisher` + `publish` + sleep）
5. **改文件后**：先读再改（编辑器可能改过）；`colcon build` 后记得重新 `source install/setup.bash`
6. **git 规范**：验证通过就提交推送；commit message 写清"改了什么+为什么"；仓库 remote 是 SSH
7. **用户环境残留**：用户终端可能自己开着 launch；动进程前先 `ps` 确认归属，别误杀用户正在用的（用户自己的 code 窗口不要动）

## 9. 关键文件索引

| 文件 | 用途 |
|---|---|
| `src/myrobot_bringup/launch/myrobot.launch.xml` | mock 一键启动（commander 已集成） |
| `src/myrobot_gazebo/launch/gazebo.launch.xml` | Gazebo 启动（gui 参数可关） |
| `src/myrobot_gazebo/worlds/myrobot_workshop.world` | 世界：地面+桌子(0.4m)+方块(0.65,0,0.45) |
| `src/myrobot_description/urdf/myrobot_urdf.xacro` | 模型入口（sim_mode 参数：mock/gazebo） |
| `src/myrobot_description/urdf/my_robot.ros2_control.xacro` | 硬件宏（插件按 sim_mode 切换） |
| `src/myrobot_description/urdf/initial_positions.yaml` | 关节初始位置（唯一来源） |
| `src/myrobot_commander_cpp/src/commander_template.cpp` | 3 话题订阅模板 |
| `src/myrobot_commander_cpp/src/grasp_demo.cpp` | 抓取演示（坐标待 gazebo 适配） |
| `src/gazebo_ros2_control/` | ⚠️ 补丁包（勿删，勿升级 apt 覆盖） |
| `stop_all.sh` | 一键清理残留进程 |
| `.vscode/c_cpp_properties.json` | VSCode 补全配置 |

## 10. Git 历史速览

```
855d048 初始工程（3 包）
1834ede commander 包 + launch 集成 + README
50dbcb0 误删恢复 + 关节控制补回
56407f9 myrobot_interfaces 自定义消息
a674c7a pose_command 位姿控制
b0d3486 修复目标混合（clearPoseTargets）
89a245f grasp_demo 抓取演示
2568fea stop_all.sh
4d9bf12 myrobot_gazebo 包 + 惯性参数 + sim_mode
0bf2290 gazebo_ros2_control 补丁 + 进度.md（当前 HEAD）
```

---

*交接完毕。祝接手顺利——核心链路已通，最后的 Gazebo 验证就差临门一脚。*
