# MyRobot 项目进度报告

> 生成日期：2026-08-20
> 仓库：https://github.com/A-CEL-ZPC/My_library（分支 main）

---

## 一、项目概况

基于 **ROS 2 Humble + MoveIt 2 + ros2_control** 的六自由度机械臂工程（6 轴 + 二指夹爪），当前为仿真验证阶段，硬件使用 mock 虚拟硬件，目标为完成 Gazebo 物理仿真与抓取应用。

## 二、已完成里程碑

| # | 里程碑 | 状态 | 说明 |
|---|---|---|---|
| 1 | 工程搭建 | ✅ | 4 个包：description / bringup / moveit_config / commander_cpp + 自定义消息包 myrobot_interfaces |
| 2 | 系统一键启动 | ✅ | `myrobot.launch.xml`：RSP + ros2_control_node + 3 控制器 + move_group + commander + rviz2 |
| 3 | 控制话题 | ✅ 实测 | `/joint_command`（6 关节角）、`/pose_command`（位姿+笛卡尔）、`/open_gripper`（夹爪开合） |
| 4 | pick-and-place 演示 | ✅ 实测 | `grasp_demo`：8 步状态机（预抓取→张开→下降→闭合→抬升→放置→下降→释放）全部通过 |
| 5 | 模型统一 | ✅ | 单一 URDF 源；初始位置由 `initial_positions.yaml` 驱动；支持 `sim_mode` 切换硬件插件 |
| 6 | 惯性参数 | ✅ | 11 个 link 全部补齐 `<inertial>`（Gazebo 物理必需） |
| 7 | Gazebo 包 | ✅ 结构完成 | `myrobot_gazebo`：世界文件（桌子+目标方块）+ launch + 控制器配置 |
| 8 | 版本兼容补丁 | ✅ 已编译 | gazebo_ros2_control 0.4.10 源码补丁，适配 ros2_control 2.54 |
| 9 | git 管理 | ✅ | 全部代码已提交推送；`stop_all.sh` 一键清理残留进程 |

## 三、Gazebo 集成进展（当前焦点）

### 遇到的问题：官方仓库版本不匹配

- 系统 `ros2_control` 被官方 Humble 源升级到 **2.54.0**（新版），而 `gazebo_ros2_control` 仍是旧版 **0.4.10**
- 新版删除了旧插件基类 `gazebo_ros2_control::GazeboSystemInterface` 的支持
- 官方源没有配套的新版 gazebo_ros2_control → Gazebo 无法直接使用

### 解决方案：源码补丁（已实现）

将 gazebo_ros2_control 0.4.10 源码放入工作区 `src/gazebo_ros2_control/`，打 3 处补丁：

| # | 位置 | 修改 |
|---|---|---|
| 1 | `gazebo_hardware_plugins.xml` | `base_class_type` → `hardware_interface::SystemInterface` |
| 2 | `CMakeLists.txt` | 增加 `pluginlib_export_plugin_description_file(hardware_interface ...)`（插件注册到新版索引键） |
| 3 | `src/gazebo_system.cpp` | `PLUGINLIB_EXPORT_CLASS` 基类改为 `hardware_interface::SystemInterface`（工厂按基类分桶注册，这是关键） |

### 已验证的进展（按时间顺序）

- ✅ 插件被 pluginlib 正确发现
- ✅ `ros2_control_node` 成功加载并初始化硬件 `Arm`（隔离测试）
- ✅ gzserver 正常启动，`/spawn_entity` 服务可用（可写 HOME 下）
- ✅ 理解 0.4.10 架构：gazebo 插件在 gzserver **内部自建 controller_manager**，无需外部 ros2_control_node
- ✅ URDF gazebo 模式生成 `<gazebo>` 插件块（带控制器配置参数）
- ⏳ **最后一步**：完整 launch 的端到端运行验证（沙箱环境 HOME 只读导致 gzserver 崩溃，需在用户真实终端验证）

## 四、已知问题 / 待办

| 优先级 | 事项 | 说明 |
|---|---|---|
| 高 | Gazebo 完整运行验证 | 在用户终端执行 `ros2 launch myrobot_gazebo gazebo.launch.xml` 验证 |
| 中 | grasp_demo 坐标适配 | mock 与 gazebo 世界物体位置不同，抓取坐标需按桌面高度（0.4m）调整 |
| 中 | 补全 package.xml 依赖 | myrobot_bringup 缺 xacro/controller 依赖声明（不阻塞运行） |
| 低 | sensors_3d.yaml 残留 | moveit 配置里的 Kinect 模板残留，无相机可删除 |
| 低 | move_group 退出段错误 | MoveIt 已知问题，仅 Ctrl+C 时出现，不影响使用 |

## 五、下一步计划

1. **Gazebo 收尾**：用户终端验证完整 launch → 控制器加载 → 机械臂在物理环境运动
2. **物理抓取**：调整 grasp_demo 坐标，验证真实物理夹取（物体掉落/碰撞）
3. **MoveIt 进阶**：规划场景避障（桌子/障碍物加入 Planning Scene）
4. **视觉感知**（可选）：相机 + AprilTag → 视觉引导抓取

## 六、常用命令速查

```bash
# 启动（mock 仿真）
ros2 launch myrobot_bringup myrobot.launch.xml

# 启动（Gazebo 物理仿真）
ros2 launch myrobot_gazebo gazebo.launch.xml

# 抓取演示
ros2 run myrobot_commander_cpp grasp_demo

# 话题控制
ros2 topic pub -1 /joint_command example_interfaces/msg/Float64MultiArray "{data: [0.5, 0.5, 0.5, 0.5, 0.5, 0.5]}"
ros2 topic pub -1 /pose_command myrobot_interfaces/msg/PoseCommand "{x: 0.7, y: 0.0, z: 0.4, roll: 3.14, pitch: 0.0, yaw: 0.0, cartesian_path: false}"
ros2 topic pub -1 /open_gripper example_interfaces/msg/Bool "{data: true}"

# 清理残留进程
./stop_all.sh
```
