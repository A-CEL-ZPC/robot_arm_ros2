# MyRobot 项目进度报告

> 更新日期：2026-08-21
> 仓库：https://github.com/A-CEL-ZPC/My_library（分支 main）

---

## 一、项目概况

基于 **ROS 2 Humble + MoveIt 2 + ros2_control** 的六自由度机械臂工程（6 轴 + 二指夹爪）。mock 仿真链路完整可用；**Gazebo 物理仿真已端到端跑通**（world 离线加载 → spawn → 控制器 → move_group → grasp_demo 8 步执行）；物理抓取已验证可夹起方块，搬运稳定性待继续优化。

## 二、已完成里程碑

| # | 里程碑 | 状态 | 说明 |
|---|---|---|---|
| 1 | 工程搭建 | ✅ | description / bringup / moveit_config / commander_cpp + myrobot_interfaces |
| 2 | 系统一键启动（mock） | ✅ | `myrobot.launch.xml`：RSP + ros2_control_node + 3 控制器 + move_group + commander + rviz2 |
| 3 | 控制话题 | ✅ 实测 | `/joint_command`、`/pose_command`、`/open_gripper` |
| 4 | pick-and-place 演示（mock） | ✅ 实测 | `grasp_demo` 8 步状态机全部通过 |
| 5 | 模型统一 + sim_mode 切换 | ✅ | 单一 URDF 源；`initial_positions.yaml` 驱动初值 |
| 6 | 惯性参数 | ✅ | 11 个 link 全部补齐 `<inertial>` |
| 7 | **Gazebo 世界离线化** | ✅ | 内联地面/阳光，不再访问在线模型数据库（原卡点） |
| 8 | **Gazebo 插件注册修复** | ✅ | 注册基类改回 `GazeboSystemInterface`，gzserver 内插件正常加载硬件 |
| 9 | **Gazebo 端到端 launch** | ✅ 实测 | spawn✅ → 3 控制器 Configured and activated✅ → move_group✅ → /joint_states 86Hz✅ |
| 10 | **grasp_demo（gazebo 模式）** | ✅ 实测 | 8 步全部规划+执行成功（笛卡尔路径，每步 ~1.3s） |
| 11 | **物理抓取（夹起方块）** | 🟡 | 已验证方块 z 0.45→0.69 被夹起；**搬运中会被甩飞，不稳定** |
| 12 | git 管理 | ✅ | 已提交推送（当前 14 个文件待提交，见对接文档 §12） |

## 三、Gazebo 集成：问题与修复全记录

### 3.1 版本兼容（ros2_control 2.54 vs gazebo_ros2_control 0.4.10）

上一轮把插件注册基类改为 `hardware_interface::SystemInterface`（为外部节点隔离测试），**破坏了 gzserver 内插件加载**（0.4.10 插件用 `ClassLoader<GazeboSystemInterface>` 按基类分桶查找）。本轮改回：

| # | 位置 | 修改 |
|---|---|---|
| 1 | `gazebo_hardware_plugins.xml` | `base_class_type` → `gazebo_ros2_control::GazeboSystemInterface` |
| 2 | `src/gazebo_system.cpp` | `PLUGINLIB_EXPORT_CLASS` 基类同步改回 |

原理：gzserver 流程 = 插件 ClassLoader 加载 → `initSim()` → `ResourceManager::import_component()` 直连导入，不走 ResourceManager 的 pluginlib 查找；`GazeboSystemInterface` 本身继承 `SystemInterface`。

### 3.2 世界文件离线化

`model://ground_plane` / `model://sun` → 内联 `<model>` / `<light>`，gzserver 不再查询 models.gazebosim.org（沙箱/离线环境会永久挂起）。

### 3.3 仿真时钟对齐（use_sim_time）

gzserver 内 controller_manager 强制仿真时钟 → /joint_states 带仿真时间戳 → move_group 墙钟判定状态过期（`couldn't receive full current joint state`）。修复：
- `move_group.launch.py` 重写，支持 `use_sim_time` 参数（默认 false，mock 不变）
- `gazebo.launch.xml`：RSP / move_group / rviz2 全部 `use_sim_time=true`

### 3.4 物理抓取优化（7 项，按序）

1. 手指碰撞盒 0.2→0.02（模型 bug，10 倍宽）
2. 夹爪 gazebo 模式 `position_pid` 力控（SetForce，避免 SetPosition 瞬移挤飞物体）+ URDF PID 增益（pos_kp=500/ki=0/kd=15）
3. `gazebo_system.cpp` 控制方式改 per-joint（原类级变量会让所有关节变力控、无增益关节瘫软）
4. **双指显式控制**：Gazebo 11 不执行 URDF mimic（右指自由漂移）；MoveIt 不把 mimic 关节写入轨迹 → 删除 mimic、右指进 ros2_control、SRDF/控制器配置/moveit_controllers 同步加右指
5. 手指+方块高摩擦（mu=5），防滑脱
6. grasp_demo gazebo 模式全笛卡尔路径（避免 OMPL 手腕翻转长路径甩飞方块）
7. **抓取高度补偿**：Gazebo 固定关节 lumping 丢失 hand→tool 的 2cm 偏移（手指实际低 2cm）→ 抓取高度 tool z=0.51（手指实际 [0.43,0.51] 完整包住方块 [0.425,0.475]）；预抓取 0.63 / 抬升 0.63 / 放置 (0.55,-0.15,0.51)

## 四、已知问题 / 待办

| 优先级 | 事项 | 说明 |
|---|---|---|
| 高 | 物理抓取搬运不稳定 | 方块可被夹起，但搬运段被甩飞；**① 关节空间高位路径会扫到方块**；笛卡尔部分失败根因未明（详见对接文档 §7.4，有候选方向） |
| 中 | 把桌子/方块加入 MoveIt Planning Scene | 从根上避免规划路径碰撞方块（对接文档 §7.4 推荐方向） |
| 中 | 当前 14 个文件未提交 | 确认后 git 提交推送（建议 message 见对接文档 §12） |
| 低 | sensors_3d.yaml 残留 | moveit 配置里 Kinect 模板残留，无相机可删除（octomap 报错非阻塞） |
| 低 | move_group 退出段错误 | MoveIt 已知问题，仅 Ctrl+C 时出现 |

## 五、下一步计划

1. **物理抓取稳定化**（对接文档 §7.4 三个方向）：规划场景避障 → ① 笛卡尔化 → 查明笛卡尔部分失败原因
2. 用户终端 GUI 完整验收：`ros2 launch myrobot_gazebo gazebo.launch.xml` + `ros2 run myrobot_commander_cpp grasp_demo gazebo`
3. MoveIt 进阶：规划场景避障
4. 视觉感知（可选）：相机 + AprilTag → 视觉引导抓取

## 六、常用命令速查

```bash
# 启动（mock 仿真）
ros2 launch myrobot_bringup myrobot.launch.xml

# 启动（Gazebo 物理仿真，默认带 GUI）
ros2 launch myrobot_gazebo gazebo.launch.xml

# 抓取演示（world 参数：mock 默认 / gazebo）
ros2 run myrobot_commander_cpp grasp_demo gazebo

# 话题控制
ros2 topic pub -1 /joint_command example_interfaces/msg/Float64MultiArray "{data: [0.5, 0.5, 0.5, 0.5, 0.5, 0.5]}"
ros2 topic pub -1 /pose_command myrobot_interfaces/msg/PoseCommand "{x: 0.7, y: 0.0, z: 0.4, roll: 3.14, pitch: 0.0, yaw: 0.0, cartesian_path: false}"
ros2 topic pub -1 /open_gripper example_interfaces/msg/Bool "{data: true}"

# 清理残留进程
./stop_all.sh
```

## 七、给下一个智能体的提醒

- **详细交接文档在 `对接文档.md`**（含全部踩坑、证据日志、测试脚本索引、物理抓取遗留问题与候选方向）
- 沙箱内 DDS 跨 bash 命令不通：集成测试必须单命令内完成（参考 `.ros_home_tmp/e2e_test.sh`）
- 所有会写 `~/.ros`/`~/.gazebo` 的命令必须 `export HOME=/home/zhz/robot_arm_ws/.ros_home_tmp`
- 残留 gzserver 占 11345 端口会导致新 gzserver 秒退；验证后必须彻底清理进程
- `gazebo_ros2_control` 是本地补丁包，勿 apt 覆盖
