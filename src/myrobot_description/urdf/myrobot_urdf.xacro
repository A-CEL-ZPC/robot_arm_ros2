<?xml version="1.0"?>
<robot name="my_robot" xmlns:xacro="http://www.ros.org/wiki/xacro">

    <!-- sim_mode: mock=虚拟硬件(默认) | gazebo=物理仿真 -->
    <xacro:arg name="sim_mode" default="mock"/>

    <xacro:include filename="common_properties.xacro"/>
    <xacro:include filename="arm.xacro"/>
    <xacro:include filename="gripper.xacro"/>
    <xacro:include filename="my_robot.ros2_control.xacro"/>

    <!-- 生成 ros2_control 硬件块，初始位置从 initial_positions.yaml 读取 -->
    <xacro:my_robot_ros2_control name="Arm"
         initial_positions_file="$(find myrobot_description)/urdf/initial_positions.yaml"
         sim_mode="$(arg sim_mode)"/>

    <!-- Gazebo 模式：加载控制插件（gzserver 内部自建 controller_manager） -->
    <xacro:property name="sim_mode_value" value="$(arg sim_mode)"/>
    <xacro:if value="${sim_mode_value == 'gazebo'}">
        <gazebo>
            <plugin name="gazebo_ros2_control" filename="libgazebo_ros2_control.so">
                <parameters>$(find myrobot_gazebo)/config/ros2_controllers.yaml</parameters>
            </plugin>
        </gazebo>
    </xacro:if>

    <joint name="gripper_base_joint" type="fixed">
        <parent link="tool_link"/>
        <child link="gripper_base_link"/>
        <origin xyz="0.0 0.0 0.0" rpy="0.0 0.0 0.0"/>
    </joint>

    <!-- 手指高摩擦（mu=5）：Gazebo 默认 mu=1 偏低，夹爪力控夹持时方块容易
         从指间滑脱；提高摩擦后夹持更可靠（配合 POSITION_PID 力控夹爪） -->
    <gazebo reference="gripper_left_finger_link">
        <mu1>5.0</mu1>
        <mu2>5.0</mu2>
    </gazebo>
    <gazebo reference="gripper_right_finger_link">
        <mu1>5.0</mu1>
        <mu2>5.0</mu2>
    </gazebo>
</robot>
